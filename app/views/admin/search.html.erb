<h3>Search results:</h3>
<ul class="no-bullets">
  <% @students.each do |student| %>
    <li> <%= link_to "#{student.full_name}", summary_admin_index_path(family_id: student.family.id) %> </li>
  <% end %>
</ul>
